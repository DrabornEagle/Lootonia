name: DKD Supabase Function Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/send-courier-order-alert/**'
      - 'supabase/config.toml'
      - '.github/workflows/dkd_supabase_function_deploy.yml'

jobs:
  dkd_deploy_send_courier_order_alert:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      DKD_SUPABASE_PROJECT_REF: ${{ secrets.DKD_SUPABASE_PROJECT_REF }}
    steps:
      - name: Dkd checkout
        uses: actions/checkout@v4

      - name: Dkd setup supabase cli
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Dkd verify function path
        run: |
          test -f supabase/functions/send-courier-order-alert/index.ts

      - name: Dkd deploy function
        run: |
          supabase functions deploy send-courier-order-alert \
            --project-ref "$DKD_SUPABASE_PROJECT_REF" \
            --use-api
